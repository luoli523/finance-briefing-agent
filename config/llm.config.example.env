# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🤖 LLM 深度分析配置文件
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 使用方法:
# 1. 复制此文件到项目根目录的 .env 文件
#    cp config/llm.config.example.env .env
#
# 2. 根据需要选择一个提供商，取消注释并填写 API Key
#
# 3. 运行分析:
#    npm run analyze:enhanced
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎯 基础配置
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 是否启用 LLM 增强分析
# true = 启用（使用 LLM）, false = 禁用（仅使用规则引擎）
LLM_ENABLED=true


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔥 方案 1: Google Gemini（推荐：免费且强大！）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 获取 API Key: https://aistudio.google.com/apikey
# 成本: Gemini 2.0 Flash 完全免费，1.5 Flash 仅 $0.07/年
# 质量: ⭐⭐⭐⭐⭐
# 速度: ⚡⚡⚡ 极快
#
# 取消下面的注释以启用：

# LLM_PROVIDER=google
# LLM_MODEL=gemini-2.0-flash-exp
# LLM_API_KEY=your-google-api-key-here

# 其他可选模型:
# LLM_MODEL=gemini-1.5-flash      # 超低价 $0.07/年
# LLM_MODEL=gemini-1.5-pro        # 高级版 $1.80/年


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 💎 方案 2: OpenAI GPT（性价比之王）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 获取 API Key: https://platform.openai.com/api-keys
# 成本: GPT-4o-mini $0.18/年, GPT-4o $2.92/年
# 质量: ⭐⭐⭐⭐⭐
# 速度: ⚡⚡⚡ 快
#
# 取消下面的注释以启用：

# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o-mini
# LLM_API_KEY=sk-your-openai-api-key-here

# 其他可选模型:
# LLM_MODEL=gpt-4o                # 最新旗舰 $2.92/年
# LLM_MODEL=gpt-4-turbo           # GPT-4 Turbo $10.95/年
# LLM_MODEL=o1-preview            # 推理专家 $18.25/年 (适合复杂分析)


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🧠 方案 3: Anthropic Claude（推理之王）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 获取 API Key: https://console.anthropic.com/
# 成本: Claude 3.5 Sonnet $7.30/年
# 质量: ⭐⭐⭐⭐⭐
# 速度: ⚡⚡ 中快
#
# 取消下面的注释以启用：

# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-3-5-sonnet-20241022
# LLM_API_KEY=sk-ant-your-anthropic-api-key-here

# 其他可选模型:
# LLM_MODEL=claude-3-opus-20240229   # Claude 3 Opus
# LLM_MODEL=claude-3-haiku-20240307  # Claude 3 Haiku (快速)


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 💰 方案 4: DeepSeek（超便宜）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 获取 API Key: https://platform.deepseek.com/
# 成本: DeepSeek Chat/R1 仅 $0.73/年
# 质量: ⭐⭐⭐⭐ (R1 推理能力 ⭐⭐⭐⭐⭐)
# 速度: ⚡⚡ 快
#
# 取消下面的注释以启用：

# LLM_PROVIDER=deepseek
# LLM_MODEL=deepseek-chat
# LLM_API_KEY=sk-your-deepseek-api-key-here

# 其他可选模型:
# LLM_MODEL=deepseek-reasoner      # R1 推理模型（适合复杂分析）


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🏠 方案 5: Ollama（本地部署，完全免费）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 安装: brew install ollama
# 启动: ollama serve &
# 下载模型: ollama pull qwen2.5:7b
# 成本: 完全免费（本地运行）
# 质量: ⭐⭐⭐⭐
# 速度: ⚡ 慢（取决于硬件）
#
# 取消下面的注释以启用：

# LLM_PROVIDER=ollama
# LLM_MODEL=qwen2.5:7b
# 无需 API Key（本地运行）

# 其他可选模型:
# LLM_MODEL=deepseek-r1:7b        # DeepSeek R1 本地版（推理强）
# LLM_MODEL=llama3.1:8b           # Llama 3.1
# LLM_MODEL=gemma2:9b             # Google Gemma 2

# 如果 Ollama 运行在非默认端口，设置 Base URL:
# LLM_BASE_URL=http://localhost:11434


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ⚙️ 高级配置（可选）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 温度参数 (0.0-2.0)
# 越低越保守，越高越创造性
# 默认: 0.7
# LLM_TEMPERATURE=0.7

# 最大输出 tokens
# 默认: 4096
# LLM_MAX_TOKENS=4096

# 请求超时时间（毫秒）
# 默认: 60000 (60秒)
# LLM_TIMEOUT=60000

# 使用自定义 Prompt（可选）
# true = 使用 prompts/custom/ 目录的自定义 prompt
# false = 使用默认 prompt
# LLM_CUSTOM_PROMPTS=false


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📊 其他必需的 API Keys（数据收集）
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Finnhub API Key (股票新闻)
# 获取: https://finnhub.io/
FINNHUB_API_KEY=your-finnhub-api-key

# FRED API Key (宏观经济数据)
# 获取: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your-fred-api-key

# Alpha Vantage API Key (可选，备用数据源)
# 获取: https://www.alphavantage.co/support/#api-key
# ALPHA_VANTAGE_API_KEY=your-alpha-vantage-api-key

# SEC User Agent (SEC EDGAR 数据)
# 格式: YourAppName/version (your-email@example.com)
SEC_USER_AGENT=FinanceBriefingAgent/1.0 (your-email@example.com)


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 💡 快速配置建议
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 场景                     推荐配置
# ──────────────────────────────────────────────────────────────
# 🏆 日常监控（免费）      Google Gemini 2.0 Flash
# 💎 性价比最高            Google Gemini 1.5 Flash ($0.07/年)
# 🚀 追求质量              OpenAI GPT-4o 或 Claude 3.5 Sonnet
# 🧠 复杂推理              DeepSeek R1 或 OpenAI o1-preview
# 🔐 隐私优先              Ollama (本地部署)
# 💰 预算有限              DeepSeek Chat ($0.73/年)
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
