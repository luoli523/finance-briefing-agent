# 🧠 LLM 增强分析器

## 概述

LLM 增强分析器采用**混合模式**，结合规则引擎和大语言模型的优势：

1. **规则引擎（Rule Engine）**: 快速、免费、可靠的基础分析
2. **LLM 深度分析（Optional）**: 深度洞察、多维推理、情景分析

---

## 🏗️ 架构设计

```
数据收集
  ↓
规则引擎分析（基础层）
  • 宏观经济
  • Fed 政策
  • 地缘政治
  • 监管环境
  • 行业分析
  ↓
LLM 深度分析（增强层，可选）
  • 深度解读
  • 多维推理
  • 情景分析
  • 投资策略
  ↓
增强简报
```

---

## 支持的 LLM 提供商

| 提供商 | 模型 | 成本 | 速度 | 质量 |
|-------|------|------|------|------|
| **OpenAI** | GPT-4 Turbo | $0.01-0.03/1K tokens | 中 | ⭐⭐⭐⭐⭐ |
| **Anthropic** | Claude 3.5 Sonnet | $0.003-0.015/1K tokens | 快 | ⭐⭐⭐⭐⭐ |
| **DeepSeek** | DeepSeek Chat | $0.00014/1K tokens | 快 | ⭐⭐⭐⭐ |
| **Ollama** | Qwen2.5/Llama3.1 | 免费（本地） | 慢 | ⭐⭐⭐ |

---

## 快速开始

### 1. 配置 LLM（可选）

编辑 `.env` 文件：

```bash
# 启用 LLM 增强
LLM_ENABLED=true

# 选择提供商和模型
LLM_PROVIDER=openai
LLM_MODEL=gpt-4-turbo

# API Key
LLM_API_KEY=sk-your-api-key-here
```

### 2. 运行混合分析

```bash
# 方式 1: 仅使用规则引擎（默认，免费）
npm run analyze:intelligent

# 方式 2: 混合模式（规则引擎 + LLM，推荐）
npm run analyze:enhanced
```

---

## 详细配置

### OpenAI

```bash
LLM_ENABLED=true
LLM_PROVIDER=openai
LLM_MODEL=gpt-4-turbo           # 或 gpt-4, gpt-3.5-turbo
LLM_API_KEY=sk-...              # 从 https://platform.openai.com/api-keys 获取
```

**成本估算**: $0.02-0.05/次分析

### Anthropic Claude

```bash
LLM_ENABLED=true
LLM_PROVIDER=anthropic
LLM_MODEL=claude-3-5-sonnet-20241022
LLM_API_KEY=sk-ant-...          # 从 https://console.anthropic.com/ 获取
```

**成本估算**: $0.01-0.03/次分析

### DeepSeek（推荐：性价比最高）

```bash
LLM_ENABLED=true
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-chat
LLM_API_KEY=sk-...              # 从 https://platform.deepseek.com/ 获取
```

**成本估算**: $0.001-0.003/次分析（非常便宜！）

### Ollama（本地模型，完全免费）

首先安装 Ollama 并下载模型：

```bash
# 安装 Ollama (macOS)
brew install ollama

# 启动 Ollama 服务
ollama serve

# 下载模型（另一个终端）
ollama pull qwen2.5:7b
```

配置：

```bash
LLM_ENABLED=true
LLM_PROVIDER=ollama
LLM_MODEL=qwen2.5:7b            # 或 llama3.1:8b
# 本地模型无需 API key
```

**成本**: 完全免费！

---

## LLM 增强的深度洞察

LLM 会提供以下深度分析：

### 1. 宏观经济深度解读
- 复杂经济现象的因果分析
- 长期趋势判断
- 政策影响的多维度评估

### 2. Fed 政策深度分析
- 未来政策路径预测
- 非常规货币政策影响
- 通胀与就业的权衡分析

### 3. 地缘政治深度解读
- 事件时间线分析
- 多国博弈推演
- 黑天鹅风险识别

### 4. 行业趋势深度分析

#### AI
- 竞争格局演变
- 技术突破的投资影响
- 长期投资逻辑

#### 半导体
- 供需周期判断
- 地缘风险对供应链的影响
- 设备与材料的投资机会

#### 数据中心
- AI 算力需求测算
- 基础设施瓶颈分析
- 能源供应链机会

#### 能源
- 能源转型路径
- 核能复兴的催化剂
- 政策支持的持续性

### 5. 跨领域深度洞察
- 系统性连接识别
- 新兴投资叙事
- 逆向思考观点
- 黑天鹅风险

### 6. 投资策略建议
- 短期（1-3个月）
- 中期（3-12个月）
- 长期（1-3年）
- 风险管理
- 投资组合配置

### 7. 情景分析
- **牛市情景**: 概率、触发因素、目标位
- **基准情景**: 概率、预期结果
- **熊市情景**: 概率、触发因素、保护措施

### 8. 关键问题和行动项
- 需要关注的关键问题
- 可执行的行动项
- 监控指标

---

## 成本对比

### 每日运行成本（假设每天1次分析）

| 提供商 | 每次成本 | 每月成本 | 每年成本 |
|-------|---------|---------|---------|
| OpenAI GPT-4 Turbo | $0.03 | $0.90 | $10.95 |
| Anthropic Claude 3.5 | $0.02 | $0.60 | $7.30 |
| DeepSeek | $0.002 | $0.06 | $0.73 |
| Ollama (本地) | $0 | $0 | $0 |

**推荐**:
- **预算有限**: Ollama（免费）或 DeepSeek（超便宜）
- **追求质量**: Claude 3.5 Sonnet 或 GPT-4 Turbo
- **平衡性价比**: DeepSeek（质量好 + 成本低）

---

## 使用示例

### 示例 1: 使用 DeepSeek（推荐）

```bash
# 1. 配置 .env
cat >> .env << EOF
LLM_ENABLED=true
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-chat
LLM_API_KEY=your-deepseek-api-key
EOF

# 2. 运行混合分析
npm run analyze:enhanced
```

### 示例 2: 使用本地 Ollama（免费）

```bash
# 1. 启动 Ollama
ollama serve &

# 2. 配置 .env
cat >> .env << EOF
LLM_ENABLED=true
LLM_PROVIDER=ollama
LLM_MODEL=qwen2.5:7b
EOF

# 3. 运行混合分析
npm run analyze:enhanced
```

### 示例 3: 使用 OpenAI

```bash
# 1. 配置 .env
cat >> .env << EOF
LLM_ENABLED=true
LLM_PROVIDER=openai
LLM_MODEL=gpt-4-turbo
LLM_API_KEY=sk-your-openai-key
EOF

# 2. 运行混合分析
npm run analyze:enhanced
```

---

## 输出文件

混合分析会生成：

```
data/processed/
└── enhanced-analysis-YYYY-MM-DDTHH-MM-SS.json
    ├── 规则引擎分析结果
    ├── LLM 深度洞察
    └── LLM 元数据（成本、tokens等）
```

---

## 常见问题

### Q: LLM 增强需要多长时间？
A: 
- OpenAI/Anthropic: 10-30 秒
- DeepSeek: 5-15 秒
- Ollama: 30-120 秒（取决于硬件）

### Q: 我应该选择哪个提供商？
A: 
- **新手**: DeepSeek（便宜 + 简单）
- **高级用户**: Claude 3.5 Sonnet（质量最好）
- **离线使用**: Ollama（完全本地）

### Q: 可以不启用 LLM 吗？
A: 可以！默认就是不启用。规则引擎已经提供了专业级的分析。

### Q: LLM 分析的质量如何？
A: 
- **GPT-4/Claude 3.5**: 优秀，专业级深度
- **DeepSeek**: 良好，性价比极高
- **Ollama 本地模型**: 中等，但完全免费

### Q: 如何控制成本？
A: 
1. 使用 DeepSeek（超便宜）
2. 按需启用（不是每次都运行）
3. 使用本地 Ollama（完全免费）
4. 减少 `LLM_MAX_TOKENS`

---

## 最佳实践

### 1. 混合使用
```bash
# 日常监控：只用规则引擎（免费）
npm run analyze:intelligent

# 重要决策：启用 LLM 增强（深度洞察）
LLM_ENABLED=true npm run analyze:enhanced
```

### 2. 成本优化
- 每周只在关键时刻（如 FOMC 会议、重要财报日）启用 LLM
- 平时使用规则引擎即可

### 3. 提供商选择
- **每日使用**: DeepSeek 或 Ollama
- **关键决策**: Claude 3.5 Sonnet 或 GPT-4

---

## 技术细节

### 提示词工程

LLM 使用了精心设计的提示词：
- 明确的角色定位（资深金融分析师）
- 结构化的输出格式（JSON）
- 基于事实的分析要求
- 多维度交叉验证

### 错误处理

- LLM 调用失败 → 自动降级到规则引擎分析
- JSON 解析失败 → 使用默认结构
- 超时保护 → 可配置超时时间

### 隐私保护

- 所有数据处理在您控制的环境中
- Ollama 完全本地，数据不出本地
- 其他提供商遵循各自的隐私政策

---

## 未来增强

- [ ] 支持更多 LLM 提供商（Gemini, LLaMA等）
- [ ] 流式输出（实时看到 LLM 分析过程）
- [ ] 分析结果缓存（避免重复调用）
- [ ] 自定义提示词模板
- [ ] 多模型集成投票（majority voting）

---

## 获取帮助

- 查看主文档: `README.md`
- 查看示例配置: `.env.example`
- 提交 Issue: GitHub Issues

---

**祝您投资顺利！** 📈💰
